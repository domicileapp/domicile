name: github

on:
  pull_request:
  issues:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v4
        with:
          sync-labels: true
          repo-token: ${{ secrets.GITHUB_TOKEN }}
